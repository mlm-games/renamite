fn main() {
    // Placeholder runner. The real entry point calls
    // `repose_platform::run_desktop_app` with the `renamite_ui::EditorShell`.
    let _doc = renamite_model::Document {
        format_version: 1,
        compositions: Default::default(),
        nodes: Default::default(),
        assets: Default::default(),
        main: Default::default(),
    };
    println!("renamite");
}