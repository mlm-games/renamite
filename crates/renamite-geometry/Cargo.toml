[package]
name = "renamite-geometry"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "VectorPath, kurbo adapters, and Graphite geometry facades for renamite."

[dependencies]
kurbo.workspace = true
glam.workspace = true
serde.workspace = true
thiserror.workspace = true
linesweeper.workspace = true

[features]
# Gate the Graphite/linesweeper boolean-backend façade. Enable once a real
# Graphite commit is pinned; plain feature so the git dep stays opt-in.
graphite-bool = []
