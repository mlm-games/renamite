[package]
name = "renamite-behavior-common"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "ToolContext, selection, snapping, and view transforms shared by renamite behaviors."

[dependencies]
renamite-model = { path = "../renamite-model" }
renamite-history = { path = "../renamite-history" }
renamite-animation = { path = "../renamite-animation" }
glam.workspace = true
serde.workspace = true