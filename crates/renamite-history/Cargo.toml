[package]
name = "renamite-history"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "EditorCommand, transactions, and undo/redo for renamite."

[dependencies]
serde.workspace = true
smallvec.workspace = true
slotmap.workspace = true
thiserror.workspace = true
renamite-animation = { path = "../renamite-animation" }
renamite-model = { path = "../renamite-model" }
renamite-geometry = { path = "../renamite-geometry" }

[dev-dependencies]
glam.workspace = true